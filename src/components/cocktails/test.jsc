{/* 
    <div className="user-cocktail-container">
      <div className="img">
       <img
          src={userCocktails.Image}
          alt="img"
          className="user-cocktail-image"
        />
      </div>
      <h4>Name: {userCocktails.Name}</h4>
      <div className="measure-ingredient-user">
        <ul className="ing-list">
          <h5>Ingredients</h5>
          {userCocktails.Ingredients.map((ing, i) => (
            <li key={i}>{ing}</li>
          ))}
        </ul>
        <ul className="meas-list">
          <h5>Measure</h5>
          {userCocktails.Measures.map((measure, i) => (
            <li key={i}>{measure}</li>
          ))}
        </ul>
      </div>
      <h5>Recipe</h5>
      <p>{userCocktails.Instructions}</p>
    </div> */}






    